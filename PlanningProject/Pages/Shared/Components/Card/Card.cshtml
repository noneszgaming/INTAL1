@model CardViewModel


<div class="w-24 h-40 rounded-[15px] flex items-center justify-center 
    @(Model.isRevealed && Model.isRevealed ? "bg-cstm-purple" :
      Model.isRevealed && !Model.isRevealed ? "bg-transparent border-4 border-cstm-purple" :
      Model.isVoted ? "bg-cstm-purple" : "bg-gray-400")
">
    @if (Model.isRevealed)
    {
        <p class="text-3xl font-bold text-cstm-purple @(Model.isRevealed ? "text-white" : "")">@Model.value</p>

    }
</div>